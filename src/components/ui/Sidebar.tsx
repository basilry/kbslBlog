import { ReactElement } from "react"
import styles from "@styles/ui/sidebar.module.scss"

function Sidebar(): ReactElement {
    return <div className={styles.sidebarWrapper}>사이드바</div>
}

export default Sidebar
